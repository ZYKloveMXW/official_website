import Vue from 'vue'
import Vuex from 'Vuex'

Vue.use(Vuex);

const store = new Vuex.Store({
    // state == data
    state: {
    },
    // getters 是对state中的数据进行处理
    getters: {
    },
    // mutations 改变state里面的数据
    mutations: {
    }
})

export default store;